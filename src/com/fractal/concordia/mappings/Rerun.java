/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.fractal.concordia.mappings;

import java.io.FileNotFoundException;
import java.io.IOException;

import org.apache.commons.codec.EncoderException;

/**
 *
 * @author saumya.goyal
 */
public class Rerun {

	public static void doingRerun(String[] args) throws EncoderException, FileNotFoundException, IOException {
		/*

int no_levels =3;        // Integer.parseInt(args[0]);
        String lev ="Manufacture,Brand,Sub-Brand";  // args[1];
        String levels[] = lev.split(",");
        String args
        int i = 0;
        int[][] column = new int[no_levels][];

        for (i = 0; i < no_levels; i++) {
            String str1 = args[2 + i];

            String[] str = str1.split(",");
            int[] col = new int[str.length];
            for (int j = 0; j < str.length; j++) {
                column[i][j] = Integer.parseInt(str[j]);
            }
        }
        System.out.println(levels);
        System.out.println(column[0]);
        System.out.println(column[1]);
        System.out.println(column[2]);

		 */
	}

}
